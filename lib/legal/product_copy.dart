/// Canonical user-facing product name.
///
/// Engine identifiers and asset filenames may still use `FirstSign` (one word).
/// Anything a homeowner or contractor reads is [displayName].
abstract final class ProductCopy {
  static const displayName = 'First Sign';

  static const tagline = 'First Sign — AI property companion';

  static const companionLabel = 'First Sign AI';
}
