# Scenario: `paint_clear`

Clear paint film failure / peel evidence.

## Drop real photos

1. Prefer close-ups of peeling paint + one elevation context shot.
2. Update `manifest.json` `photos[]` if filenames change.
3. Optionally tighten `forbiddenCategories` once paint detection is stable.

## Run

```bash
flutter test test/calibration_harness_test.dart
```
